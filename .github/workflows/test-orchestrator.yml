name: Test Orchestrator Call
on:
  workflow_dispatch:
jobs:
  test:
    uses: jbcom/control-center/.github/workflows/ecosystem-orchestrator.yml@main
    with:
      action: review
      repository: agentic-dev-library/control
      pr_number: 49
    secrets:
      CI_GITHUB_TOKEN: ${{ secrets.CI_GITHUB_TOKEN }}
